{
  "name": "@chatui/cloudflare-template",
  "version": "0.1.0",
  "description": "Cloudflare Workers deployment template for ChatUI widgets",
  "type": "module",
  "private": true,
  "scripts": {
    "prebuild": "node scripts/build-widgets.mjs",
    "dev": "pnpm run prebuild && wrangler dev",
    "build": "vite build",
    "deploy": "wrangler deploy",
    "build-deploy": "pnpm run build && pnpm run deploy",
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit",
    "check": "pnpm run typecheck && pnpm run build && wrangler deploy --dry-run"
  },
  "dependencies": {
    "@chatui/widgets": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.25.2",
    "agents": "^0.2.13",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.13.12",
    "@cloudflare/workers-types": "^4.20260107.1",
    "@types/node": "^25.0.3",
    "fast-glob": "^3.3.3",
    "typescript": "^5.9.3",
    "vite": "^6.4.1",
    "wrangler": "^4.56.0"
  },
  "cloudflare": {
    "bindings": {
      "WORKER_DOMAIN_BASE": {
        "description": "The domain of this worker where assets will be served. Example: https://my-chatui-widgets.example.workers.dev"
      },
      "WIDGET_DOMAIN": {
        "description": "The widget domain for OpenAI sandbox. Example: https://example.com"
      }
    }
  }
}
