{
  "name": "@chatui/mcp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "pnpm -C ../web build:widget && pnpm -C ../../packages/widgets build",
    "dev": "node server.js",
    "prestart": "pnpm -C ../web build:widget && pnpm -C ../../packages/widgets build",
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.2",
    "zod": "^3.25.76"
  }
}
