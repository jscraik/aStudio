# Typography

Use typography tokens from `@astudio/tokens` for font size, weight, line height, and letter spacing.

Examples:

- `var(--type-body-md-font-size)`
- `var(--type-body-md-line-height)`
- `var(--type-body-md-font-weight)`

Typography tokens align with Apps SDK UI foundations and should be used consistently.
